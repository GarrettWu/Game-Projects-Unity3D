using UnityEngine;
using System.Collections;

public class Test : MonoBehaviour {

	// Use this for initialization
	void Start () 
	{
//		rigidbody.AddForce(-30.0f, 0, 0);
	}
	
	// Update is called once per frame
	void Update () {
//		rigidbody.AddForce(-1.0f, 0, 0);
		transform.RotateAround(new Vector3(0, 10.0f, 0), Vector3.forward, 2.0f);
	}
}
